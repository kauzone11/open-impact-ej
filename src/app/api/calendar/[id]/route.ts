import { prisma } from "@/lib/prisma";
import { requireHubPermission } from "@/lib/hub/auth";
import { HubApiError, hubJson, withHubApi } from "@/lib/hub/api";
import { updateCalendarEvent } from "@/lib/hub/calendar-service";
type Context = { params: Promise<{ id: string }> };
export const PATCH = withHubApi<Context>(async (request, context) => { const session = await requireHubPermission("collaboration:access"); const { id } = await context.params; const body = await request.json().catch(() => null) as Record<string, unknown> | null; if (!body || !Number.isInteger(Number(body.version))) throw new HubApiError("Versão obrigatória.", 400); const event = await updateCalendarEvent(prisma, { ...session, timezone: session.organization.timezone }, id, { version: Number(body.version), title: typeof body.title === "string" ? body.title : undefined, description: typeof body.description === "string" || body.description === null ? body.description : undefined, startAt: typeof body.startAt === "string" ? new Date(body.startAt) : undefined, endAt: typeof body.endAt === "string" ? new Date(body.endAt) : undefined, allDay: typeof body.allDay === "boolean" ? body.allDay : undefined, timezone: typeof body.timezone === "string" ? body.timezone : undefined, location: typeof body.location === "string" || body.location === null ? body.location : undefined, archive: body.action === "archive" }); return hubJson({ event }); });
