import { prisma } from "@/lib/prisma";
import { requireHubPermission } from "@/lib/hub/auth";
import { HubApiError, hubJson, withHubApi } from "@/lib/hub/api";
import { createCalendarEvent, getCalendar } from "@/lib/hub/calendar-service";
export const GET = withHubApi(async (request) => { const session = await requireHubPermission("collaboration:access"); const query = new URL(request.url).searchParams; const from = new Date(query.get("from") || ""); const to = new Date(query.get("to") || ""); if (Number.isNaN(from.getTime()) || Number.isNaN(to.getTime())) throw new HubApiError("Período inválido.", 400); return hubJson(await getCalendar(prisma, { ...session, timezone: session.organization.timezone }, { from, to, directorateId: query.get("directorateId") || undefined })); });
export const POST = withHubApi(async (request) => { const session = await requireHubPermission("collaboration:access"); const body = await request.json().catch(() => null) as Record<string, unknown> | null; if (!body || typeof body.title !== "string") throw new HubApiError("Dados inválidos.", 400); const startAt = new Date(String(body.startAt || "")); const endAt = new Date(String(body.endAt || "")); const event = await createCalendarEvent(prisma, { ...session, timezone: session.organization.timezone }, { title: body.title, description: typeof body.description === "string" ? body.description : null, startAt, endAt, allDay: body.allDay === true, timezone: typeof body.timezone === "string" ? body.timezone : undefined, location: typeof body.location === "string" ? body.location : null, directorateId: typeof body.directorateId === "string" ? body.directorateId : null, projectId: typeof body.projectId === "string" ? body.projectId : null }); return hubJson({ event }, { status: 201 }); });
